{
  "name": "mystream",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node server.mjs",
    "refresh:archive": "node scripts/refresh-archive.mjs",
    "build": "node scripts/refresh-archive.mjs && vite build"
  },
  "dependencies": {
    "vite": "8.2.2"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
